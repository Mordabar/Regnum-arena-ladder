<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="top.regnumarenaladder.app" android:compileSdkVersion="34" android:compileSdkVersionCodename="14">

    <!-- Para encontrar el navegador que sabe abrir la app a pantalla completa
         (Chrome u otro compatible). Desde Android 11 hay que declararlo. -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <!-- La mitad de la relacion con el sitio. La otra mitad es
             /.well-known/assetlinks.json en regnumarenaladder.top: con las dos,
             Chrome abre el sitio sin barra de direcciones. -->
        <meta-data android:name="asset_statements" android:resource="@string/asset_statements" />

        <activity
            android:name="top.regnumarenaladder.app.Lanzador"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="regnumarenaladder.top" />
            </intent-filter>
        </activity>
    </application>
</manifest>
